# VELIN — MBTI 향수 브랜드 사이트 계획안

## 1. 프로젝트 개요
- **브랜드명**: VELIN (@_velin_office)
- **컨셉**: 16가지 MBTI 성격 유형에서 영감을 받은 니치 향수 브랜드
- **참고 사이트**: gelcandleshop.co.kr (모바일 쇼핑몰 UI 무드 참고)
- **콘텐츠 원본**: `향수 PPT.pdf` (총 7페이지 브랜드 무드보드)

## 2. PDF 원본 분석 요약
| 페이지 | 내용 |
|---|---|
| 1 | 히어로 — "THE VALUE OF FRAGRANCE", "WHERE SCENT MEETS EMOTION" |
| 2 | About Us — 향수 조향 스튜디오/작업실 이미지, 브랜드 소개 문구 |
| 3 | Style Categories — Casual / Formal / Sporty / Trendy 4분류 스타일 일러스트 |
| 4 | Fragrance Notes — Top Notes (자몽, 베르가못, 피치, 클로브, 만다린, 블랙체리, 히노키, 라임) |
| 5 | Fragrance Notes — Middle Notes + 농도 구분 (Parfum 20–30%, EDP 15–20%) |
| 6 | Fragrance Notes — Base Notes (베티버, 패츌리, 샌달우드, 오크모스, 바닐라, 앰버, 시더우드, 머스크 등) |
| 7 | 클로징 — "PERFUME IS AN EXPERIENCE", "A SCENT THAT DEFINES YOU" |

디자인 톤: 다크그린 + 크림 컬러, 세리프 디스플레이 폰트, 스파클(별) 장식과 타원 궤도 라인 모티프. 텍스트는 대부분 Lorem ipsum 임시 카피.

## 3. 사용자와 확정한 방향
1. **사이트 형태**: 실제 결제/쇼핑몰이 아닌 **브랜드 소개형 랜딩페이지** (1페이지, 섹션 스크롤 구조)
2. **MBTI 구조화**: **16개 MBTI 유형별 개별 향수** (4개 기질군 NT/NF/SJ/SP로 그룹핑해 필터링 가능하게 구성)
3. **카피라이팅**: Lorem ipsum 대신 **직접 초안 작성** — 브랜드 스토리, 노트 설명, 16종 향수 네이밍/설명 전부 새로 작성

## 4. 사이트 구조 (index.html)
1. **Header** — 로고 VELIN, 내비게이션(About / Fragrance Notes / Collection / Contact), 모바일 햄버거 메뉴
2. **Hero** — "당신의 성격에는 어울리는 향이 있다" 카피, 다크그린 배경 + 스파클/궤도 장식, CTA 버튼(컬렉션 보기 / 인스타그램)
3. **About** — 브랜드 스토리(노트 3겹 = 성격의 3겹 비유), CSS로 만든 향수병 비주얼, 통계(16종/3겹/1개의 향)
4. **Fragrance Notes 교육 섹션** — Top/Middle/Base 노트 설명 + 노트 칩, Parfum/EDP 농도 카드
5. **Collection** — 16개 MBTI 카드 그리드, 그룹 필터(전체/분석가/외교관/관리자/탐험가), 각 카드에 향수명·설명·Top/Mid/Base 노트
6. **Closing** — "PERFUME IS AN EXPERIENCE" 클로징 카피 + CTA
7. **Footer**

## 5. 16종 MBTI 시그니처 향수 (작성 완료)
| 유형 | 향수명 | 컨셉 |
|---|---|---|
| INTJ | OBSIDIAN (옵시디언) | 전략가의 밤 |
| INTP | CIPHER (사이퍼) | 논리 속의 여백 |
| ENTJ | IMPERIUM (임페리움) | 지휘관의 결 |
| ENTP | PARADOX (패러독스) | 발상의 스파크 |
| INFJ | AURORA VEIL (오로라 베일) | 조용한 통찰 |
| INFP | MOONFIG (문피그) | 몽상가의 정원 |
| ENFJ | LUMEN (루멘) | 곁을 밝히는 사람 |
| ENFP | WILDFLOWER SPARK (와일드플라워 스파크) | 즉흥의 리듬 |
| ISTJ | ARCHIVE (아카이브) | 변하지 않는 결 |
| ISFJ | QUIET HEARTH (콰이엇 허스) | 말없는 헌신 |
| ESTJ | CORNERSTONE (코너스톤) | 체계의 중심 |
| ESFJ | GATHER (개더) | 함께의 온도 |
| ISTP | MECHANICA (메카니카) | 손끝의 감각 |
| ISFP | SILENT BLOOM (사일런트 블룸) | 고요한 감각 |
| ESTP | RUSH (러시) | 지금 이 순간 |
| ESFP | SUNCATCH (선캐치) | 무대 위의 반짝임 |

각 향수는 PDF에 실린 실제 노트 목록(그레이프프룻, 베르가못, 피치, 클로브, 만다린, 블랙체리, 히노키, 라임 / 프리지아, 네롤리, 일랑일랑, 제라늄, 오렌지플라워, 블랙페퍼, 클라리세이지, 블랙베리, 오션, 버베나, 블랙커런트, 아쿠아, 유칼립투스, 히야신스, 화이트로즈, 아이리스, 무화과 / 베티버, 패츌리, 샌달우드, 오크모스, 바닐라, 앰버, 시더우드, 머스크, 파인우드, 헬리오트로핀, 오리스콘크리트)에서 Top/Middle/Base 조합을 구성.

## 6. 홍보 목적 확정 사항
- **홍보 채널/목적**: SNS 바이럴 콘텐츠(인스타그램 등) + 포트폴리오/브랜딩 제안용, 두 느낌을 동시에 살리는 방향으로 확정
- 실사 제품 사진은 없음 → **그래픽/일러스트 스타일을 더 다듬는 방향**으로 진행 (사진 대체 없이 CSS/Canvas 비주얼 강화)
- 16개 카드 색감: **4개 기질군(NT/NF/SJ/SP)마다 다른 포인트 컬러** 적용해 그룹 구분감 + 개인 카드 공유 유도
- "향수 찾기" 기능: **내 MBTI 직접 선택** 방식 (미니테스트 아님, 개발 부담 적고 결과 도출 빠름)
- 공유 유도 방식: 결과 카드를 **이미지로 저장**할 수 있는 버튼 제공 (Web Share API 연동은 다음 단계 예정)
- 기능 배치: **히어로에 "내 향수 찾기" 버튼 → 결과 모달**, 아래엔 기존 16종 전체 그리드 유지. **그리드의 개별 카드를 클릭해도 동일한 결과 모달**이 뜨도록 상호작용 통일

## 7. 구현 현황 (완료)
- `index.html` — 전체 섹션 마크업 + 히어로 "내 향수 찾기" 버튼 + 향수 찾기 모달(피커/결과 2단계 구조) 추가 완료
- `assets/style.css` — 다크그린/크림 베이스 톤 + 반응형 스타일 완료. `.mbti-card`에 `data-group`별 CSS 변수(`--card-bg-1/2`, `--card-accent`)로 NT(다크그린+골드)/NF(버건디+로즈)/SJ(앰버브라운+크림)/SP(틸블루+크림) 4가지 테마 적용. 모달/피커그리드 스타일 추가
- `assets/script.js` —
  - 16종 카드 데이터 렌더링 + 그룹 필터 + 모바일 내비/헤더 스크롤 효과 (기존)
  - `GROUP_META`: 그룹별 라벨/컬러 데이터 추가
  - 향수 찾기 모달 로직: 피커 단계 ↔ 결과 단계 전환, ESC/오버레이 클릭으로 닫기
  - **Canvas 기반 결과 카드 생성**(`drawResultCard`, 1080×1080): 그룹 컬러 배경 + 궤도/스파클 장식 + **중앙 향수병 일러스트**(`drawBottle` — 캡/넥/바디/액체 그라데이션/유리 하이라이트/VELIN 라벨 플레이트) + 향수명/설명/노트 피라미드(3단 컬럼) + 워터마크
  - `downloadCanvasImage`: "이미지 저장" 버튼으로 `VELIN_{코드}.png` 다운로드
- 로컬 서버(`python -m http.server 8731`)로 브라우저 확인 완료, 사용자 확인 결과 "괜찮은 것 같다"는 피드백 받음

## 8. 다음 단계 (TODO) — 우선순위 순
사용자가 5가지 보완 아이디어에 전부 동의, **1번(깃허브+Vercel 배포)부터 순서대로 진행** 요청함.

**[완료] 배포 전 선행 정리**
- `.gitignore` 추가 — `향수 PPT.pdf`(80MB, 원본 무드보드 참고자료)가 **공개 저장소**에 그대로 커밋되지 않도록 `*.pdf` 제외 처리
- `assets/favicon.svg` 추가 — 다크그린(`#1b2419`) 배경 + 골드(`#c9a86a`) 세리프 "V" 모노그램, `index.html`에 `<link rel="icon">` 연결 완료

1. **[완료] GitHub 연동 + Vercel 배포**
   - GitHub 저장소: https://github.com/dsd1310-netizen/velin-mbti-perfume (Public), 계정 `dsd1310-netizen`
   - Vercel 프로젝트: `velin-mbti-perfume` (팀 `dsd1310-2119s-projects`), GitHub 저장소와 연결 완료 → 이후 `main` 브랜치 push 시 자동 배포
   - **배포 URL: https://velin-mbti-perfume.vercel.app**
   - gh CLI는 winget으로 설치, `gh auth login` / Vercel 로그인 모두 사용자가 브라우저에서 직접 인증 완료
   - git 커밋 작성자 정보 없어서 GitHub noreply 이메일로 전역 설정(`308184771+dsd1310-netizen@users.noreply.github.com`)
   - `.claude/`(로컬 도구 설정)는 `.gitignore`에 추가해 저장소에서 제외
   - ⚠️ **이슈 발견 및 수정**: 최초 배포 시 `향수 PPT.pdf`(77MB)가 `.gitignore`에는 있었지만 Vercel CLI 업로드 시엔 무시되지 않아 실제로 프로덕션 URL에서 공개 접근 가능한 상태로 배포됐었음(확인: `curl` 200 응답). `.vercelignore`(`*.pdf`, `.claude`, `계획안.md`)를 추가해 재배포로 즉시 수정, PDF가 노출됐던 이전 배포본은 `vercel rm`으로 삭제 완료. 현재 프로덕션에서 PDF/계획안.md 모두 404 확인됨
2. **[완료] 카카오톡/인스타 링크 공유 미리보기(OG 메타태그)**
   - `assets/og-image.png`(1200×630) 제작 — 브랜드 톤(다크그린 배경 + 골드/크림 텍스트 + 궤도·스파클 장식) 유지한 정적 HTML을 헤드리스 Edge로 스크린샷해 생성
   - `index.html`에 `og:title`/`og:description`/`og:image`/`og:url`/`og:locale` + `twitter:card` 등 메타태그 추가
   - 배포 후 프로덕션에서 이미지·메타태그 노출 확인 완료
3. **[완료] 모바일 공유 시트 연동(Web Share API)**
   - `navigator.share` 지원 브라우저에서만 "공유하기" 버튼 노출 (`shareBtn`, 기본 `hidden`)
   - Canvas 결과 카드를 `canvas.toBlob` → `File`로 변환해 `navigator.canShare({files})`가 true일 때 이미지 파일 그대로 공유, 미지원 시 텍스트/URL만 공유하는 폴백 처리
   - 버튼 3개(이미지 저장/공유하기/다른 유형 보기) 레이아웃은 `flex-wrap`으로 좁은 화면에서 2+1 자동 줄바꿈
4. **[완료] 인스타 스토리용 세로형(9:16) 이미지 옵션**
   - 결과 모달 상단에 "1:1 정사각형 / 9:16 스토리" 토글 버튼 추가, 클릭 시 즉시 재렌더링
   - `LAYOUTS` 설정 객체로 포맷별 텍스트 크기·병 스케일(스토리는 1.12배)·여백을 별도로 튜닝해 세로 캔버스(1080×1920)에서도 빈 공간 없이 자연스러운 배치
   - 저장 파일명(`VELIN_{코드}_{포맷}.png`)과 공유 파일에도 포맷 반영
   - 로컬 헤드리스 스크린샷으로 두 포맷 및 토글 전환 정상 동작 확인 후 배포
   - ⚠️ **버그 수정(추후 발견)**: 설명 문구 줄바꿈 길이가 유형마다 달라서, 짧은 설명일 때 9:16 카드 아래쪽에 빈 공간이 크게 남는 문제가 실사용 중 발견됨. `measureWrappedLineCount`로 설명/노트의 실제 줄바꿈 수를 먼저 측정한 뒤, 콘텐츠 블록을 캔버스 안(푸터 위 공간)에서 수직 중앙 정렬하도록 수정. 정사각형 포맷은 오프셋이 0으로 clamp되어 기존과 동일하게 유지됨
5. **[완료] "궁합" 콘텐츠 기능** — 내 MBTI + 친구 MBTI를 함께 선택해 "이 둘의 향 조합" 결과를 보여주는 바이럴용 신규 기능
   - **콘텐츠 설계**: 120가지 조합(16×16)을 손으로 쓰지 않고 ① 기존 16종 향 데이터 재사용 + ② 기질군(NT/NF/SJ/SP) 조합 문구 **10개만 신규 작성**(순서 무관이라 4개 군 페어는 10가지뿐) + ③ 두 향의 TOP/MID/BASE 첫 노트를 자동으로 짝짓는 "블렌드 노트" 규칙으로 120가지 자동 생성. `PAIR_COPY` 상수로 `assets/script.js`에 반영 완료
   - 콘텐츠 설계 검증용 인터랙티브 프로토타입(Claude Artifact): https://claude.ai/code/artifact/c5fe3ff0-cbf9-42f9-818d-e4e3d26bd10f
   - `index.html`: 헤더 nav·푸터에 "궁합" 링크, Collection과 Closing 사이 `#compat` 섹션(다크 배경), `#compatModal` 2단계 모달(피커: `#compatPickerA`/`#compatPickerB` 그리드 2개 + `#compatGoBtn` / 결과: `#compatCanvas` + 저장·공유·다시선택 버튼)
   - `assets/script.js`: `drawCompatCard(codeA, codeB)`(1080×1080, `drawResultCard`의 `LAYOUTS.square`/`drawWrappedCentered`/`drawSpark`/`drawNotesPyramid` 재사용, 병 일러스트 없이 두 그룹 색 블렌드 그라데이션 + 궤도 장식 + 블렌드 노트로 구성), `renderCompatPickers`, `downloadCompatImage`/`shareCompatImage`, `openModal`/`closeModal`을 `id` 파라미터로 일반화해 두 모달 모두 지원
   - `assets/style.css`: `.picker-btn.is-active`, `.picker-label`, `.compat-go`, `.compat` 섹션, `#compatCanvas` 반응형 규칙(`#resultCanvas`에만 있던 걸 빠뜨렸다가 로컬 테스트 중 발견해 수정)
   - `feature/compat-scent` 브랜치에서 작업 후 로컬 헤드리스 스크린샷으로 피커 선택 상태·버튼 활성화·결과 카드 렌더까지 전체 플로우 확인 → `main` 머지 → 배포 확인 완료

## 9. 전체 재검토 (2026-08-06) — 발견 및 수정한 버그
"궁합" 기능까지 배포한 뒤 사용자 요청으로 전체 재점검 진행. 로컬 헤드리스 스크린샷으로 다양한 조합(같은 코드끼리, 그룹 다른 조합, 설명 짧은/긴 유형 등)과 모달 상호작용을 다시 테스트해 아래 2가지 실제 버그를 발견해 수정·배포함:

1. **[수정 완료] 향수 찾기 모달 "X" 닫기 버튼이 동작하지 않던 버그** — `openModal`/`closeModal`을 `id` 파라미터를 받도록 일반화하면서, `modalClose` 버튼에 `closeModal`을 그대로 이벤트 핸들러로 넘기던 기존 코드가 깨짐(클릭 이벤트 객체가 `id` 인자로 들어감). 화살표 함수로 감싸 수정.
2. **[추가 완료] 모달 키보드 포커스 트랩 부재** — 모달이 열려 있어도 Tab으로 뒤에 가려진 페이지 요소로 포커스가 빠져나가는 접근성 문제. 모달 열릴 때 첫 포커스 가능 요소로 이동 + Tab이 모달 안에서만 순환하도록 트랩 추가, 닫을 때 원래 포커스 위치로 복귀.

검증 완료(문제 없음으로 확인):
- 다크그린 배경(`#1b2419`) 위 크림(`#f6ecd9`)·골드(`#c9a86a`) 텍스트, 크림 배경(`#faf5ea`) 위 잉크색 텍스트 모두 WCAG 명암비 7:1 이상(AAA 기준 충족)
- `PAIR_COPY` 10개 키가 16×16 모든 조합이 필요로 하는 기질군 페어 10가지와 정확히 일치(빠짐/오타 없음, 로직으로 검증)
- 헤더 nav 5개 항목(궁합 추가 후)이 좁은 데스크톱 폭(~800px)에서도 줄바꿈 없이 들어감, 모바일 햄버거 드로어에도 정상 표시
- 궁합 카드 노트 블렌딩이 가장 긴 조합("오리스콘크리트" 등)에서도 줄바꿈으로 자연스럽게 처리되어 레이아웃 깨짐 없음

## 10. 폰트 교체 + 노트 목록 누락 보완 (2026-08-06)
- **폰트**: 한글 헤드라인에 쓰던 `Noto Serif KR`이 크게 쓰이면 궁서체처럼 보인다는 피드백 → **Gowun Batang**으로 교체. `--serif` CSS 변수뿐 아니라 Canvas에 하드코딩된 한글 폰트 지정 2곳(결과 카드 부제 `drawResultCard`, 궁합 카드 헤드라인 `drawCompatCard`)도 같이 수정 — CSS 변수를 안 타는 곳이라 놓치기 쉬움. Gowun Batang은 400/700 두 웨이트만 있어 관련 텍스트는 700(bold)으로 통일.
- **노트 목록 누락 보완**: 두 군데에서 원본 PDF 무드보드 기준 전체 36개 노트(Top 8·Middle 17·Base 11) 중 일부가 빠져 있던 것을 발견해 수정:
  1. `index.html`의 "Fragrance Notes" 교육 섹션 칩 목록이 원래 일부만 보여주는 큐레이션이었음(Top 6/8, Middle 7/17, Base 6/11) → 36개 전부로 확장
  2. 16개 향수의 `top`/`mid`/`base` 데이터 전체를 훑어보니 **히노키**가 TOP 카테고리 노트인데 어느 향수의 top에도 없이 INTJ의 mid에만 있었고, **오션**은 어디에도 전혀 쓰이지 않았음 → ISTP(top: 라임,자몽→라임,히노키 — "그린 우디" 컨셉과 더 잘 맞음)와 ESTP(mid: 블랙커런트,버베나→블랙커런트,오션 — "뛰어드는" 컨셉과 어울림)에 각각 배치해 36개 노트 전부가 최소 한 곳엔 쓰이도록 보완(로직으로 재검증 완료)

## 11. 심화 교재(전문 조향 이론 및 MBTI) 콘텐츠 반영 (2026-08-07)
`[심화 교재] 전문 조향 이론 및 MBTI 맞춤형 레시피 통합 가이드` PDF를 검토해, 브랜드 카피 톤에 맞는 부분만 골라 기존 섹션에 자연스럽게 녹였음(레시피 배합비·안전성 정보·진로 로드맵 등 소비자 랜딩페이지와 무관한 내용, 그리고 기존 NT/NF/SJ/SP 4분류와 충돌하는 MBTI-4원소 매칭표는 제외):
- **About**: 후각-변연계 과학적 사실(0.5초 만에 뇌 도달) + 숙성 공정(무거운 순서로 쌓기, 1~2주 저온 숙성) 문단 추가, `about-stats`에 "1~2주 저온 숙성 기간" 4번째 항목 추가
- **Fragrance Notes**: Top/Middle/Base 설명에 분자군·지속 시간(모노테르펜 10~30분 등) 근거 보강
- **신규 서브섹션 "SCENT JOURNEY"**: 5단계 시향 가이드(0분/1분/10분 골든타임/30분/3시간+) 카드 추가
- **신규 서브섹션 "SCENT & EMOTION"**: 6대 향기 계열(시트러스~스파이시)별 감정 메시지 6카드 그리드 추가
- 헤드리스 Chrome(CDP)으로 데스크톱/모바일 풀페이지 스크린샷 검증 → 모바일에서 `about-stats` 4개 항목이 3+1로 어색하게 줄바꿈되는 것 발견, 480px 이하에서 2×2 그리드로 전환하도록 수정 후 재검증 완료

## 12. 대전 팝업스토어 방문 신청폼 추가 (2026-08-07)
사용자가 "질문을 1개씩 해달라"고 요청해 AskUserQuestion으로 8라운드 확인 후 구현. 확정된 방향: 대전 오프라인 팝업스토어 방문 예약 목적, 데이터는 구글시트 자동 누적, 폼은 구글폼이 아닌 사이트와 동일한 브랜드 톤 커스텀 폼, 배치는 기존 궁합/향수찾기와 동일한 새 섹션+버튼→모달 패턴, 헤더 네비에 "신청" 링크 추가(총 6개).
- **필드**: 이름(텍스트) / 나이(숫자 1~120) / 전화번호(010-0000-0000 형식 검증) / 지역(대전 5구 드롭다운 + "대전 외 지역", 선택해도 제출은 막지 않음—추후 운영자가 수동 필터링) / 개인정보 수집·이용 동의(필수 체크박스)
- **이벤트 정보**: 날짜·장소 미확정이라 "확정되는 대로 안내" 문구로 TBD 처리, 추후 실제 일정 나오면 카피만 교체하면 됨
- **`index.html`**: `#apply` 섹션(compat과 closing 사이, paper 배경으로 넣어 dark-light-dark-light 리듬 유지) + `#applyModal`(폼 단계/완료 단계 2-step, 기존 모달 패턴 재사용) + 봇 방지용 숨김 허니팟 필드(`website`)
- **`assets/style.css`**: `.field`/`.field-checkbox`/`.apply-submit` 등 폼 스타일, `.hp-field`로 허니팟 시각적 숨김
- **`assets/script.js`**: `applyOverlay`를 기존 `openModal`/`closeModal`/Escape 핸들러에 합류, `applyForm` submit 시 클라이언트 검증(전화번호 정규식, 동의 체크) → `/api/apply`로 fetch → 성공 시 완료 단계 표시, 실패 시 에러 문구 표시 + 버튼 재활성화
- **`api/apply.js`**: Vercel 서버리스 함수(신규, 의존성 0개). `googleapis` 패키지 대신 Node 내장 `crypto`로 서비스 계정 JWT(RS256)를 직접 서명해 구글 OAuth 토큰 교환 → Sheets API `values:append`로 "신청내역" 탭에 행 추가. 서버 측에서도 이름/나이/전화번호/지역/동의 재검증, 허니팟 필드가 채워져 있으면 시트에 쓰지 않고 가짜 성공 응답. 필요한 환경변수 2개: `GOOGLE_SERVICE_ACCOUNT_JSON`(다운로드한 서비스 계정 JSON 키 파일 전체를 그대로 붙여넣기 — JSON.parse가 private_key 안의 이스케이프된 줄바꿈을 알아서 풀어주므로 수동 `\n` 치환 없이도 안전함), `GOOGLE_SHEET_ID`(구글시트 탭 이름은 반드시 "신청내역"이어야 함)
- ⚠️ **버그 발견·수정**: 최초 배포 후 실제 신청 테스트 중 500 에러 발생 → Vercel 프로덕션 로그(`vercel logs --query 'status:500'`)에서 `DECODER routines::unsupported`(OpenSSL PEM 파싱 실패) 확인. `GOOGLE_SERVICE_ACCOUNT_EMAIL`+`GOOGLE_PRIVATE_KEY` 2개 변수로 나눠 받던 방식이 Vercel 환경변수 입력 과정에서 줄바꿈이 깨지기 쉬운 구조였음 → 서비스 계정 JSON 파일 전체를 통째로 하나의 변수(`GOOGLE_SERVICE_ACCOUNT_JSON`)로 받고 `JSON.parse`에 위임하는 방식으로 리팩터링해 근본 원인 제거
- 헤드리스 Chrome으로 폼 열기·입력·제출(로컬엔 API가 없어 에러 상태로 정상 표시되는 것까지) 데스크톱/모바일 모두 스크린샷 검증 완료
- Google Cloud 서비스 계정 설정은 사용자가 직접 콘솔에서 진행(GCP 로그인 필요해 대행 불가) — 프로젝트 생성 → Sheets API 활성화 → 서비스 계정+JSON 키 발급 → 시트에 서비스 계정 이메일 편집자로 공유 → Vercel 환경변수 3개 등록 순으로 안내함. 구글시트 ID는 시트 URL의 `/d/`와 `/edit` 사이 문자열(공개 저장소라 이 문서엔 실제 값 기록하지 않음)
- ⚠️ 이 저장소는 Public이라, 실제 `GOOGLE_SHEET_ID`·서비스 계정 키 값은 Vercel 환경변수에만 두고 이 문서나 커밋에는 절대 남기지 않을 것
- 실 프로덕션 엔드포인트로 테스트 신청 1건을 보내 시트에 정상 기록되는 것까지 확인 후 사용자가 테스트 행 삭제 확인

## 13. 신청폼 필드 교체 + 폰트를 Jua로 변경 (2026-08-07)
- **필드 교체**: "지역(대전 구 드롭다운)" → "수령 가능 날짜"(`<input type="date">`, `min`을 오늘 날짜로 JS에서 동적 설정)로 교체. `api/apply.js`도 `REGIONS` 검증을 날짜 형식·과거 날짜 거부 검증으로 교체, 구글시트 5번째 컬럼 값도 지역 대신 날짜로 저장(시트 헤더 5번째 칸도 사용자가 "수령 가능 날짜"로 직접 수정 필요)
- **폰트**: "글씨가 딱딱하다"는 피드백으로 한글 헤드라인 폰트를 Gowun Batang → **Jua**(더 동글동글하고 캐주얼한 느낌)로 교체. `--serif` 변수 + Canvas 하드코딩 2곳(`drawResultCard`, `drawCompatCard`) 함께 수정. Jua는 웨이트가 400 하나뿐이라 기존에 강제하던 700(bold) 지정은 제거
- ⚠️ **버그 발견·수정**: 교체 후 스크린샷 검증 중 About 통계 "1~2주"와 Scent Journey 단계 라벨("0분"·"10분"·"3시간+" 등)에서 숫자(Playfair Display)와 한글 단위(Jua 폴백)가 겹쳐 보이는 렌더링 버그 발견. 두 폰트가 같은 텍스트 런 안에서 폴백 경계를 만들 때 자간 계산이 깨지는 것으로 추정 → 한글 단위 글자만 `<span class="unit">`으로 분리해 `var(--sans)`(Noto Sans KR)를 명시 지정, 텍스트 런을 강제로 끊어 해결. 전체 페이지 재스크린샷으로 다른 숫자+한글 혼합 지점 없는지 확인(`[0-9][가-힣]` 정규식으로 전수 검색) 후 재검증 완료

## 14. 신청폼 지역→날짜, 대전 표현 정리, 카드 위계 조정, 나이 제거+시간 추가 (2026-08-07)
연속된 소규모 피드백 라운드:
- "대전 거주자" 문구 삭제(신청 섹션 lead, 모달 sub 2곳)
- "VELIN 팝업스토어" → "VELIN 여름 이벤트"로 브랜딩 변경, "대전에서, 먼저 만나요" 헤드라인도 대전 언급 없는 "이 여름, 먼저 만나요"로 교체(섹션이 휑해 보여서 헤드라인 자체는 유지하기로 결정)
- 본문에서 "안내드립니다"가 "안 / 내드립니다"로 줄바꿈되는 문제 발견 → `body`에 `word-break: keep-all` 전역 추가로 해결(사이트 전체 한글 문단에 적용되는 근본 수정)
- **Collection MBTI 카드 위계 반전**: 기존엔 향수명(`.name`, 26px)이 MBTI 코드(`.code`, 15px)보다 훨씬 커서 MBTI가 안 보인다는 피드백 → `.code`를 32px/700굵기로 키우고 `.name`은 16px 대문자 라벨로 축소, 시각적 우선순위를 MBTI 코드 중심으로 반전
- **신청폼 필드 변경**: "나이" 필드 완전 삭제(HTML/JS/서버 검증/시트 컬럼 모두), "수령 가능 날짜" 아래에 "수령 가능 시간"(`<input type="time">`, `HH:MM` 24시간制 정규식 검증) 신규 추가. 구글시트 컬럼 순서가 A:타임스탬프 B:이름 C:전화번호 D:수령가능날짜 E:수령가능시간 F:동의로 바뀌어서 **시트 헤더 행도 이 순서로 다시 수정 필요**(사용자에게 안내함)
- 배포 후 사용자가 시트 헤더를 새 컬럼 순서로 직접 수정 완료, 실 프로덕션 엔드포인트로 테스트 신청(`[테스트] 확인용3`, 나이 없이/수령 시간 포함)을 보내 `{"ok":true}` 200 응답 + Vercel 로그 500 없음 확인. 이번 라운드(섹션 12~14) 변경 사항 전부 검증 완료 상태

## 15. 신청폼 전화번호 대시 필수 해제 + 시트 RAW 저장 + 헤드라인 폰트(푸른전남체) + 실사진 갤러리 (2026-08-09)
- **전화번호**: `pattern="010-\d{4}-\d{4}"`가 모바일에서 "-" 입력이 안 되는 키패드 때문에 제출을 막는다는 피드백 → `pattern` 제거, placeholder는 예시로만 유지. 클라이언트·서버 모두 숫자만 추출(`replace(/\D/g,'')`)해 `010` + 8자리인지만 검증하고, 저장 직전에 `010-0000-0000` 형태로 정규화(어떻게 입력해도 시트엔 항상 같은 포맷으로 들어감)
- ⚠️ **버그 발견·수정**: 사용자가 실제로 시트를 확인해보니 수령 가능 날짜/시간 컬럼이 `46243`, `0.6666666667` 같은 raw 일련번호로 저장되어 있었음 → Sheets API `valueInputOption=USER_ENTERED`가 "2026-09-01"/"14:30" 같은 문자열을 날짜·시간 값으로 해석해 시리얼 넘버로 변환한 것이 원인. `RAW`로 변경해 문자열 그대로 저장되도록 근본 수정(단, 이 수정 전에 이미 들어간 기존 행은 소급 적용 안 되므로 수동으로 재입력 필요하다고 안내함)
- **헤드라인 폰트**: 사용자가 전라남도 무료 폰트 "푸른전남체" 스크린샷을 요청 → Noonnu(무료 한글 폰트 모음)가 jsDelivr로 호스팅하는 CDN 링크(`cdn.jsdelivr.net/gh/projectnoonnu/noonfonts_two@1.0/PureunJeonnam.woff`)를 웹서치로 확인 후 실제 curl로 200 응답·파일 크기 검증하고 적용. 로고·MBTI 코드·카드 이름·About/Collection/신청 섹션 헤드라인은 그대로 두고, 사용자가 스크린샷으로 짚어준 5곳(히어로/노트 인트로/시향가이드·감정 소제목 2개/궁합/클로징)에만 `@font-face` + 선택자로 정밀 적용
- **실사진 갤러리**: 사용자가 `향수사진/` 폴더에 실제 향수병 촬영본 5장을 올려줌 — 기존에 임시로 만들어뒀던 "SCENT & EMOTION"(6대 향기 계열 카드) 서브섹션을 통째로 걷어내고, 그 자리에 5장을 한 눈에 보여주는 `.photo-grid`(데스크톱 5열 → 태블릿 3열 → 모바일 2열, `aspect-ratio: 3/4` + `object-fit: cover`로 통일)로 교체. 원본은 960~1024×1280·최대 467KB였던 걸 PIL로 폭 720px·JPEG quality 82로 리사이즈해 `assets/gallery/`에 저장(총 1.15MB → 717KB). 원본 `향수사진/` 폴더는 `.gitignore`/`.vercelignore`에 추가해 저장소·배포본에서 제외(용량 큰 원본은 로컬에만 유지, `향수 PPT.pdf`와 동일한 처리 방식)

## 16. 갤러리 사진 교체 + About 로고 이미지 적용 (2026-08-09)
사용자가 향수사진 폴더에 사진 4장(ogin.jpg/ping.jpg/eina.jpg/logo.jpg)을 추가로 올림. 요청 문장이 다소 모호해 AskUserQuestion으로 순서 해석과 색감(밝은 핑크·티비 파스텔이 사이트의 어두운 포레스트그린 톤과 다름)을 먼저 확인 후 진행.
- **갤러리 재배치**: 기존 2번(장미+VELIN 워터마크)은 삭제, 기존 4번(식물·자갈 속 한 병)이 2번 자리로 이동, 3~5번은 신규 사진(ogin=감귤류 바구니, ping=핑크 마카롱, eina=티비색 디저트 접시)으로 교체. 기존 3번·5번 사진은 자연스럽게 갤러리에서 빠짐. 색감이 사이트 톤과 다른 걸 사용자에게 미리 확인했고, "그대로 넣어도 괜찮다"는 답을 받아 보정 없이 그대로 반영
- **About 섹션 비주얼**: 기존 CSS로 그린 향수병 목업(`.bottle`/`.bottle-cap`/`.bottle-neck`/`.bottle-body`)을 사용자가 준 `logo.jpg`(다크그린 배경의 VELIN 워드마크 + "SCENT & ATELIER" 태그라인, 1280×1280 정사각형) 이미지로 교체. 로고의 정사각 비율에 맞춰 `.bottle-card`의 `aspect-ratio`를 4/5 → 1/1로 변경, 기존 CSS 목업 관련 규칙은 전부 제거하고 `.logo-image`(object-fit: cover)로 대체 — 로고 배경색이 카드 그라데이션과 거의 비슷해 이음매 없이 자연스럽게 붙음
- 모든 신규 이미지는 기존 갤러리와 동일하게 PIL로 리사이즈·재압축(720px 폭/quality 82, 로고는 640×640/quality 85)해 `assets/gallery/`에 저장

기타 보류 항목 (사용자 판단/외부 자원 필요):
- [ ] 실 구매 연결이 필요해지면 별도 쇼핑몰 플랫폼(카페24 등) 연동 논의
- [ ] 헤더 "Contact" 메뉴가 실제 연락 수단(이메일 등) 없이 인스타 링크뿐인 클로징 섹션으로 연결됨 — 포트폴리오 제안용 목적 고려 시 실제 연락처 노출 여부 검토
- [ ] Vercel 커스텀 도메인 연결 여부 (기본 `*.vercel.app`로 유지할지 별도 구매할지)
- [ ] 방문자/공유 성과 측정용 분석 도구(GA4 등) 도입 여부 — SNS 바이럴+포트폴리오 목적이라 성과 증빙에 필요할 수 있음
- [ ] "VELIN" 브랜드명 기존 상표 사용 여부 간단 확인
