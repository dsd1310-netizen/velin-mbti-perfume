# VELIN — MBTI 향수 브랜드 사이트 계획안

## 1. 프로젝트 개요
- **브랜드명**: VELIN (@_velin_office)
- **컨셉**: 16가지 MBTI 성격 유형에서 영감을 받은 니치 향수 브랜드
- **참고 사이트**: gelcandleshop.co.kr (모바일 쇼핑몰 UI 무드 참고)
- **콘텐츠 원본**: `향수 PPT.pdf` (총 7페이지 브랜드 무드보드)

## 2. PDF 원본 분석 요약
| 페이지 | 내용 |
|---|---|
| 1 | 히어로 — "THE VALUE OF FRAGRANCE", "WHERE SCENT MEETS EMOTION" |
| 2 | About Us — 향수 조향 스튜디오/작업실 이미지, 브랜드 소개 문구 |
| 3 | Style Categories — Casual / Formal / Sporty / Trendy 4분류 스타일 일러스트 |
| 4 | Fragrance Notes — Top Notes (자몽, 베르가못, 피치, 클로브, 만다린, 블랙체리, 히노키, 라임) |
| 5 | Fragrance Notes — Middle Notes + 농도 구분 (Parfum 20–30%, EDP 15–20%) |
| 6 | Fragrance Notes — Base Notes (베티버, 패츌리, 샌달우드, 오크모스, 바닐라, 앰버, 시더우드, 머스크 등) |
| 7 | 클로징 — "PERFUME IS AN EXPERIENCE", "A SCENT THAT DEFINES YOU" |

디자인 톤: 다크그린 + 크림 컬러, 세리프 디스플레이 폰트, 스파클(별) 장식과 타원 궤도 라인 모티프. 텍스트는 대부분 Lorem ipsum 임시 카피.

## 3. 사용자와 확정한 방향
1. **사이트 형태**: 실제 결제/쇼핑몰이 아닌 **브랜드 소개형 랜딩페이지** (1페이지, 섹션 스크롤 구조)
2. **MBTI 구조화**: **16개 MBTI 유형별 개별 향수** (4개 기질군 NT/NF/SJ/SP로 그룹핑해 필터링 가능하게 구성)
3. **카피라이팅**: Lorem ipsum 대신 **직접 초안 작성** — 브랜드 스토리, 노트 설명, 16종 향수 네이밍/설명 전부 새로 작성

## 4. 사이트 구조 (index.html)
1. **Header** — 로고 VELIN, 내비게이션(About / Fragrance Notes / Collection / Contact), 모바일 햄버거 메뉴
2. **Hero** — "당신의 성격에는 어울리는 향이 있다" 카피, 다크그린 배경 + 스파클/궤도 장식, CTA 버튼(컬렉션 보기 / 인스타그램)
3. **About** — 브랜드 스토리(노트 3겹 = 성격의 3겹 비유), CSS로 만든 향수병 비주얼, 통계(16종/3겹/1개의 향)
4. **Fragrance Notes 교육 섹션** — Top/Middle/Base 노트 설명 + 노트 칩, Parfum/EDP 농도 카드
5. **Collection** — 16개 MBTI 카드 그리드, 그룹 필터(전체/분석가/외교관/관리자/탐험가), 각 카드에 향수명·설명·Top/Mid/Base 노트
6. **Closing** — "PERFUME IS AN EXPERIENCE" 클로징 카피 + CTA
7. **Footer**

## 5. 16종 MBTI 시그니처 향수 (작성 완료)
| 유형 | 향수명 | 컨셉 |
|---|---|---|
| INTJ | OBSIDIAN (옵시디언) | 전략가의 밤 |
| INTP | CIPHER (사이퍼) | 논리 속의 여백 |
| ENTJ | IMPERIUM (임페리움) | 지휘관의 결 |
| ENTP | PARADOX (패러독스) | 발상의 스파크 |
| INFJ | AURORA VEIL (오로라 베일) | 조용한 통찰 |
| INFP | MOONFIG (문피그) | 몽상가의 정원 |
| ENFJ | LUMEN (루멘) | 곁을 밝히는 사람 |
| ENFP | WILDFLOWER SPARK (와일드플라워 스파크) | 즉흥의 리듬 |
| ISTJ | ARCHIVE (아카이브) | 변하지 않는 결 |
| ISFJ | QUIET HEARTH (콰이엇 허스) | 말없는 헌신 |
| ESTJ | CORNERSTONE (코너스톤) | 체계의 중심 |
| ESFJ | GATHER (개더) | 함께의 온도 |
| ISTP | MECHANICA (메카니카) | 손끝의 감각 |
| ISFP | SILENT BLOOM (사일런트 블룸) | 고요한 감각 |
| ESTP | RUSH (러시) | 지금 이 순간 |
| ESFP | SUNCATCH (선캐치) | 무대 위의 반짝임 |

각 향수는 PDF에 실린 실제 노트 목록(그레이프프룻, 베르가못, 피치, 클로브, 만다린, 블랙체리, 히노키, 라임 / 프리지아, 네롤리, 일랑일랑, 제라늄, 오렌지플라워, 블랙페퍼, 클라리세이지, 블랙베리, 오션, 버베나, 블랙커런트, 아쿠아, 유칼립투스, 히야신스, 화이트로즈, 아이리스, 무화과 / 베티버, 패츌리, 샌달우드, 오크모스, 바닐라, 앰버, 시더우드, 머스크, 파인우드, 헬리오트로핀, 오리스콘크리트)에서 Top/Middle/Base 조합을 구성.

## 6. 홍보 목적 확정 사항
- **홍보 채널/목적**: SNS 바이럴 콘텐츠(인스타그램 등) + 포트폴리오/브랜딩 제안용, 두 느낌을 동시에 살리는 방향으로 확정
- 실사 제품 사진은 없음 → **그래픽/일러스트 스타일을 더 다듬는 방향**으로 진행 (사진 대체 없이 CSS/Canvas 비주얼 강화)
- 16개 카드 색감: **4개 기질군(NT/NF/SJ/SP)마다 다른 포인트 컬러** 적용해 그룹 구분감 + 개인 카드 공유 유도
- "향수 찾기" 기능: **내 MBTI 직접 선택** 방식 (미니테스트 아님, 개발 부담 적고 결과 도출 빠름)
- 공유 유도 방식: 결과 카드를 **이미지로 저장**할 수 있는 버튼 제공 (Web Share API 연동은 다음 단계 예정)
- 기능 배치: **히어로에 "내 향수 찾기" 버튼 → 결과 모달**, 아래엔 기존 16종 전체 그리드 유지. **그리드의 개별 카드를 클릭해도 동일한 결과 모달**이 뜨도록 상호작용 통일

## 7. 구현 현황 (완료)
- `index.html` — 전체 섹션 마크업 + 히어로 "내 향수 찾기" 버튼 + 향수 찾기 모달(피커/결과 2단계 구조) 추가 완료
- `assets/style.css` — 다크그린/크림 베이스 톤 + 반응형 스타일 완료. `.mbti-card`에 `data-group`별 CSS 변수(`--card-bg-1/2`, `--card-accent`)로 NT(다크그린+골드)/NF(버건디+로즈)/SJ(앰버브라운+크림)/SP(틸블루+크림) 4가지 테마 적용. 모달/피커그리드 스타일 추가
- `assets/script.js` —
  - 16종 카드 데이터 렌더링 + 그룹 필터 + 모바일 내비/헤더 스크롤 효과 (기존)
  - `GROUP_META`: 그룹별 라벨/컬러 데이터 추가
  - 향수 찾기 모달 로직: 피커 단계 ↔ 결과 단계 전환, ESC/오버레이 클릭으로 닫기
  - **Canvas 기반 결과 카드 생성**(`drawResultCard`, 1080×1080): 그룹 컬러 배경 + 궤도/스파클 장식 + **중앙 향수병 일러스트**(`drawBottle` — 캡/넥/바디/액체 그라데이션/유리 하이라이트/VELIN 라벨 플레이트) + 향수명/설명/노트 피라미드(3단 컬럼) + 워터마크
  - `downloadCanvasImage`: "이미지 저장" 버튼으로 `VELIN_{코드}.png` 다운로드
- 로컬 서버(`python -m http.server 8731`)로 브라우저 확인 완료, 사용자 확인 결과 "괜찮은 것 같다"는 피드백 받음

## 8. 다음 단계 (TODO) — 우선순위 순
사용자가 5가지 보완 아이디어에 전부 동의, **1번(깃허브+Vercel 배포)부터 순서대로 진행** 요청함.

**[완료] 배포 전 선행 정리**
- `.gitignore` 추가 — `향수 PPT.pdf`(80MB, 원본 무드보드 참고자료)가 **공개 저장소**에 그대로 커밋되지 않도록 `*.pdf` 제외 처리
- `assets/favicon.svg` 추가 — 다크그린(`#1b2419`) 배경 + 골드(`#c9a86a`) 세리프 "V" 모노그램, `index.html`에 `<link rel="icon">` 연결 완료

1. **[완료] GitHub 연동 + Vercel 배포**
   - GitHub 저장소: https://github.com/dsd1310-netizen/velin-mbti-perfume (Public), 계정 `dsd1310-netizen`
   - Vercel 프로젝트: `velin-mbti-perfume` (팀 `dsd1310-2119s-projects`), GitHub 저장소와 연결 완료 → 이후 `main` 브랜치 push 시 자동 배포
   - **배포 URL: https://velin-mbti-perfume.vercel.app**
   - gh CLI는 winget으로 설치, `gh auth login` / Vercel 로그인 모두 사용자가 브라우저에서 직접 인증 완료
   - git 커밋 작성자 정보 없어서 GitHub noreply 이메일로 전역 설정(`308184771+dsd1310-netizen@users.noreply.github.com`)
   - `.claude/`(로컬 도구 설정)는 `.gitignore`에 추가해 저장소에서 제외
   - ⚠️ **이슈 발견 및 수정**: 최초 배포 시 `향수 PPT.pdf`(77MB)가 `.gitignore`에는 있었지만 Vercel CLI 업로드 시엔 무시되지 않아 실제로 프로덕션 URL에서 공개 접근 가능한 상태로 배포됐었음(확인: `curl` 200 응답). `.vercelignore`(`*.pdf`, `.claude`, `계획안.md`)를 추가해 재배포로 즉시 수정, PDF가 노출됐던 이전 배포본은 `vercel rm`으로 삭제 완료. 현재 프로덕션에서 PDF/계획안.md 모두 404 확인됨
2. **[완료] 카카오톡/인스타 링크 공유 미리보기(OG 메타태그)**
   - `assets/og-image.png`(1200×630) 제작 — 브랜드 톤(다크그린 배경 + 골드/크림 텍스트 + 궤도·스파클 장식) 유지한 정적 HTML을 헤드리스 Edge로 스크린샷해 생성
   - `index.html`에 `og:title`/`og:description`/`og:image`/`og:url`/`og:locale` + `twitter:card` 등 메타태그 추가
   - 배포 후 프로덕션에서 이미지·메타태그 노출 확인 완료
3. **[완료] 모바일 공유 시트 연동(Web Share API)**
   - `navigator.share` 지원 브라우저에서만 "공유하기" 버튼 노출 (`shareBtn`, 기본 `hidden`)
   - Canvas 결과 카드를 `canvas.toBlob` → `File`로 변환해 `navigator.canShare({files})`가 true일 때 이미지 파일 그대로 공유, 미지원 시 텍스트/URL만 공유하는 폴백 처리
   - 버튼 3개(이미지 저장/공유하기/다른 유형 보기) 레이아웃은 `flex-wrap`으로 좁은 화면에서 2+1 자동 줄바꿈
4. **[완료] 인스타 스토리용 세로형(9:16) 이미지 옵션**
   - 결과 모달 상단에 "1:1 정사각형 / 9:16 스토리" 토글 버튼 추가, 클릭 시 즉시 재렌더링
   - `LAYOUTS` 설정 객체로 포맷별 텍스트 크기·병 스케일(스토리는 1.12배)·여백을 별도로 튜닝해 세로 캔버스(1080×1920)에서도 빈 공간 없이 자연스러운 배치
   - 저장 파일명(`VELIN_{코드}_{포맷}.png`)과 공유 파일에도 포맷 반영
   - 로컬 헤드리스 스크린샷으로 두 포맷 및 토글 전환 정상 동작 확인 후 배포
5. **[🚧 진행 중 — WIP] "궁합" 콘텐츠 기능** — 내 MBTI + 친구 MBTI를 함께 선택해 "이 둘의 향 조합" 결과를 보여주는 바이럴용 신규 기능
   - **콘텐츠 설계 확정**: 120가지 조합(16×16) 전부를 손으로 쓰지 않고, ① 기존 16종 향 데이터 재사용 + ② 기질군(NT/NF/SJ/SP) 조합 문구 **10개만 신규 작성**(순서 무관 조합이라 4개 군 페어는 10가지뿐) + ③ 두 향의 TOP/MID/BASE 첫 노트를 자동으로 짝짓는 "블렌드 노트" 규칙, 이 3가지 조합으로 120가지를 자동 생성
   - 이 방식을 검증하기 위해 인터랙티브 프로토타입을 만들어 확인 완료 (Claude Artifact로 발행, 브랜드 톤 재사용한 데모): https://claude.ai/code/artifact/c5fe3ff0-cbf9-42f9-818d-e4e3d26bd10f
   - **작업 브랜치**: `feature/compat-scent` (GitHub에 push 완료, `main`에는 반영 안 함 — JS 로직이 아직 없어서 그대로 배포하면 사이트가 깨진 상태가 됨. 프로덕션은 영향 없음)
   - **이 브랜치에 완료된 것 (HTML+CSS만)**:
     - `index.html`: 헤더 nav·푸터에 "궁합" 링크 추가, Collection과 Closing 사이에 새 `#compat` 섹션(다크 배경, 기존 notes-intro와 톤 통일) 추가, `#compatModal` 모달 마크업 추가 — 2단계 구조(피커 단계: `#compatStepPicker` 안에 `#compatPickerA`/`#compatPickerB` 그리드 2개 + "궁합 보기" 버튼 `#compatGoBtn` / 결과 단계: `#compatStepResult` 안에 `#compatCanvas`, 저장·공유·다시선택 버튼)
     - `assets/style.css`: `.picker-btn.is-active`(선택 상태 표시), `.picker-label`, `.compat-go`, `.compat` 섹션 스타일 추가
   - **다음 세션에서 이어서 할 것 (`assets/script.js`, 아직 미작성)**:
     1. 아래 `PAIR_COPY` 10개 문구를 스크립트에 그대로 추가 (톤은 기존 16종 향 설명과 같은 2문장 구조로 이미 다듬어둠):
        ```js
        function pairKey(gA, gB) { return [gA, gB].sort().join('-'); }
        function firstWord(str) { return str.split(',')[0].trim(); }

        const PAIR_COPY = {
          'NT-NT': { title: '전략가들의 밀도', blurb: '말보다 논리로 통하는 두 사람. 정제된 우디와 앰버가 서로의 결을 짙게 덧그린다.' },
          'NF-NT': { title: '이성과 다정함의 접점', blurb: '냉철함과 다정함이 서로를 알아보는 관계. 시더우드의 단단함에 화이트 플로럴이 스며든다.' },
          'NT-SJ': { title: '설계와 실행의 결', blurb: '방향을 정하는 사람과 그 결을 지키는 사람. 흔들림 없는 우디 앰버가 신뢰를 쌓는다.' },
          'NT-SP': { title: '질서와 즉흥의 스파크', blurb: '치밀한 계획과 예측 불가능한 순간이 부딪히는 사이. 시트러스의 스파크가 묵직한 우디를 깨운다.' },
          'NF-NF': { title: '다정함의 배가', blurb: '말하지 않아도 서로를 헤아리는 두 사람. 부드러운 플로럴이 겹겹이 쌓여 오래 남는다.' },
          'NF-SJ': { title: '온기와 신뢰의 결', blurb: '곁에 있는 것만으로 안심이 되는 관계. 따뜻한 바닐라와 앰버가 편안하게 감싼다.' },
          'NF-SP': { title: '감성과 자유의 리듬', blurb: '즉흥적인 에너지에 다정함이 스며드는 사이. 발랄한 과일향에 부드러운 머스크가 생기를 더한다.' },
          'SJ-SJ': { title: '변하지 않는 약속', blurb: '한번 맺은 신뢰를 지키는 두 사람. 단정한 우디와 바닐라가 두 배로 단단해진다.' },
          'SJ-SP': { title: '안정과 모험의 균형', blurb: '지키는 사람과 뛰어드는 사람, 팽팽하지만 서로를 완성하는 사이. 안정적인 우디에 시트러스가 긴장감을 더한다.' },
          'SP-SP': { title: '즉흥의 폭발', blurb: '망설임 없이 서로에게 뛰어드는 두 사람. 짜릿한 시트러스와 베리가 강렬한 에너지로 터진다.' },
        };
        ```
     2. `drawCompatCard(codeA, codeB)` 캔버스 렌더 함수 작성 — 기존 `drawResultCard`와 같은 패턴(1080×1080, `LAYOUTS.square`의 notes/footer 값 재사용, `drawWrappedCentered`/`drawSpark`/`hexToRgba`/`drawNotesPyramid` 그대로 재사용). 병 일러스트는 없이 두 그룹 색을 블렌드한 그라데이션 배경 + 궤도 장식 + "코드A × 코드B" + 헤드라인(`PAIR_COPY`) + 블렌드 노트로 구성(claude 아티팩트 프로토타입과 동일 레이아웃)
     3. `renderCompatPickers()` — `#compatPickerA`/`#compatPickerB`에 16개 `.picker-btn` 채우기 (기존 `renderPickerGrid()`와 동일 패턴)
     4. 이벤트 와이어링: 그리드 클릭 시 그리드별로 단일 선택 상태 유지(`is-active` 토글) + 양쪽 다 선택되면 `compatGoBtn` 활성화 → 클릭 시 결과 단계 전환 + `drawCompatCard` 호출. `downloadCompatImage`/`shareCompatImage`는 기존 `downloadCanvasImage`/`shareResultImage`와 동일 패턴으로 새로 작성(파일명 `VELIN_DUO_{코드A}_{코드B}.png`)
     5. `openModal`/`closeModal`이 현재 `scentModal`에 하드코딩되어 있어 `compatModal`도 열고 닫을 수 있도록 파라미터화 필요 (`openModal(id='scentModal')` 형태), Escape 키 핸들러도 두 모달 모두 체크하도록 확장
     6. 로컬 헤드리스 스크린샷으로 두 그리드 선택 → 결과 카드 렌더 → 저장/공유 동작 확인 후, `feature/compat-scent` 브랜치에서 커밋 → PR 또는 바로 `main`에 머지 → 배포 확인

기타 보류 항목:
- [ ] 실제 제품 사진(향수병, 스튜디오 사진 등) 확보 시 About/Hero 섹션에 교체 적용
- [ ] 실 구매 연결이 필요해지면 별도 쇼핑몰 플랫폼(카페24 등) 연동 논의
- [ ] og:image용 정적 프리뷰 이미지 제작 방법 확정 (기존 Canvas 결과 카드 재활용 vs 별도 디자인)
- [ ] "궁합" 콘텐츠(TODO 5번) 조합 카피 분량 재산정 — 16종 조합은 최소 120가지, 문구 작성량이 다른 TODO보다 클 수 있음
- [ ] 헤더 "Contact" 메뉴가 실제 연락 수단(이메일 등) 없이 인스타 링크뿐인 클로징 섹션으로 연결됨 — 포트폴리오 제안용 목적 고려 시 실제 연락처 노출 여부 검토
- [ ] Vercel 커스텀 도메인 연결 여부 (기본 `*.vercel.app`로 유지할지 별도 구매할지)
- [ ] 접근성 검증 — 다크그린 배경 텍스트 명암비, 향수 찾기 모달 키보드 포커스 트랩
- [ ] 방문자/공유 성과 측정용 분석 도구(GA4 등) 도입 여부 — SNS 바이럴+포트폴리오 목적이라 성과 증빙에 필요할 수 있음
- [ ] "VELIN" 브랜드명 기존 상표 사용 여부 간단 확인
